---
interface Props {
	date: Date;
}

let { date } = Astro.props;
date = new Date(date)

---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-uk', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
